# TODO - Mobile responsiveness (DoorDelights)

- [x] Update `style.css` with mobile-first improvements (baseline tweaks + breakpoints for <=768px and <=480px).


- [x] Update `index.html` to support a mobile nav panel (minimal HTML changes).



- [x] Update `script.js` to toggle the mobile nav open/close.



- [x] Sanity check: cart sidebar width on mobile, hero typography scaling, menu tabs wrapping.

- [ ] Run a quick local check by opening `index.html` in browser and resizing to ~390px / ~768px / ~1024px.

